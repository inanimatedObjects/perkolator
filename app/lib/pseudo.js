// PSUEDOCODE CONTRIBUTOR LOG

CORE COMPONENTS
@DATA object
@DOM object

CORE OPERATION  // => first principle of responsive web
  function bindData = (dataObj, DOM) => {
    return renderDOM(dataObj);
  }






// <============ =============== ============== ============ =========== ====>
/** @DATA OBJECT
  * @param  {rangeInputs, numberInputs, } data   // data passed in, usually from child classes





  * @return {dataObject}   // the data object loaded to global
  */











// <============ =============== ============== ============ =========== ====>
/** @DOM OBJECT
    * @param  {any} DOMElement   // the root document used to render in browsers
    * @return {dataObject}   // the data object loaded to global
    */
